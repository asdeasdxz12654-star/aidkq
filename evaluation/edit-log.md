# 수정 이력 (EditLog)

AI가 만든 결과를 사람이 고칠 때마다 한 줄씩 남긴다. 삭제하지 않고 누적한다.
같은 `유형`이 3회 이상 쌓이면 규칙집 등록을 검토하고, 등록했으면 `규칙화` 열에 규칙 코드를 적는다.

유형: `문구`(원문 불일치) · `수치`(시간·길이 값) · `누락`(기대결과 추가) · `분리`/`병합` · `사전조건` · `절차` · `우선순위` · `삭제`(무효 TC) · `조건추가`(03a 승인 단계에서 추가)

| 날짜 | 단계 | TC/조건 ID | 유형 | before | after | 이유 | 규칙화 |
|---|---|---|---|---|---|---|---|
| 2026-09-18 | 03b | (예) ED-006 | 누락 | "방이 저장되었습니다" 토스트가 노출된다 | "방이 저장되었습니다" 토스트가 2초간 노출된 후 사라진다 | 토스트 노출 시간 누락 | UI-01 |
