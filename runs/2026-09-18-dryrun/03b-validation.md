# 03b 검증 리포트 — 2026-09-18 dry run

대상: `03b-output.json` (TC 43건) · 기계 검증: `node validate-tc.js` · 표: `03b-tc-table.md`
입력: `03a-approval.md` 채택 O 39건 (근거 있는 조건 전부), X 6건은 질의로 이월

## 검증 리포트
- 총 TC: 43건 / 오류: 0건 / 경고: 4건 (모두 A01 유추 근거 — Q-17·Q-18·Q-19 답변 대기)
- **내보내기 가능 여부: 가능** — 남은 경고 4건은 모두 질의 답변 대기다. 답변 전까지 Fail 판정 근거로 쓰지 않는다.
- 최초 실행에서는 오류 3건이었다. 양식 프로파일에 "전체 흐름" 화면 약어가 없어 FLOW-001~003의 ID가 패턴에 맞지 않았고, 사용자 승인으로 `"전체 흐름": "FLOW"`를 추가해 해소했다(2026-09-19).
- 승인 대조: 채택 O 39건이 모두 TC가 됐고(GWT 분리로 4건이 2개 TC가 되어 43건), X 6건으로 만든 TC는 없다. 승인 목록에 없는 조건을 새로 만든 것도 없다.

### 위반 목록
| TC ID | 코드 | 수준 | 내용 | 수정 제안 |
|---|---|---|---|---|
| 3D-008 | A01 | 경고 | 기대결과 1의 근거가 유추 (Q-19) | 답변 전까지 Fail 판정 근거로 쓰지 않는다 |
| ED-014 | A01 | 경고 | 기대결과 2의 근거가 유추 (Q-17) | 위와 동일 |
| LIST-007 | A01 | 경고 | 기대결과 1의 근거가 유추 (Q-18) | 위와 동일 |
| FLOW-002 | A01 | 경고 | 기대결과 2의 근거가 유추 (Q-17) | 위와 동일 |

해소 이력 (2026-09-19, 사용자 승인)
- 오류 3건 — FLOW-001·002·003의 R08(양식 프로파일에 없는 화면 약어 "FLOW"). 프로파일에 약어를 추가해 해결.
- 경고 1건 — FLOW-001의 R07(기대결과 5개). 프로파일 `granularity.exceptions`에 유스케이스 예외(최대 8)를 두어 해결. 끝-끝 흐름을 나누면 검증이 깨지므로 TC는 그대로 뒀다.

R01(금지어), R02(확인 종결), R03(항목=기대결과), R04(출처 누락), R05(사전조건 누락), R06(절차에 결과 서술), R09(중복 TC), R10(인용 문구 불일치)은 위반 없음.
R10은 기대결과의 큰따옴표 인용 전부를 기획서 원문·질의 답변 원문과 대조했다(테스트 데이터 값 제외). 불일치 0건.

## AI 기반 점검

### A01 환각 검출
`quote: null`(근거를 댈 수 없는 기대결과)은 0건이다. 다만 **근거가 직접 문장이 아니라 유추인 기대결과가 4건** 있다. 기획 확인 전에는 Fail 판정 근거로 쓰지 말아야 한다.
이 4건은 `evidence[].inference`에 기록했고 `validate-tc.js`가 경고로 잡는다. 이번 실행에서 추가한 필드다 — 그전에는 `quote`만 채우면 근거 있음으로 통과했다.

| TC ID | 기대결과 | 무엇을 유추했나 | 질의 |
|---|---|---|---|
| 3D-008 | 1. 온보딩 화면으로 이동된다 | Q-10 답변은 **뒤로가기** 기준이다. 닫기(X) 후에도 "최초 진입"이 유지되는지는 직접 근거가 없다 | Q-19 |
| LIST-007 | 1. 입력창에 "거실"이 노출된다 | p.4는 "해당 방의 편집 화면으로 이동한다"까지만 적혀 있다. 저장된 이름이 입력창에 채워지는지는 미기재 | Q-18 |
| ED-014 | 2. 마지막 수정일이 노출된다 | p.4의 "마지막 수정일" 정의만 있고 **갱신 시점**이 없다. 그래서 기대결과에 날짜 값을 적지 않았다 | Q-17 |
| FLOW-002 | 2. "거실 리뉴얼", "침실" 순으로 노출된다 | 저장 시 수정일이 갱신되어야 순서가 바뀐다. 갱신 시점이 미정의라 이 순서도 전제에 의존한다 | Q-17 |

### A02 커버리지
요구사항(`02-answered.json`)의 elements·conditions·states·timers·layout 33항목을 TC와 기계 대조했다. 키워드 일치로 28항목이 연결됐고, 5항목은 키워드가 어긋나 수작업으로 확인했다. **미커버 항목은 없다.**

| 항목 | 기계 대조 | 수작업 확인 |
|---|---|---|
| REQ-01 C-01 로그인 여부 | 없음 | MENU-003(비로그인), MENU-002(로그인)로 커버 |
| REQ-02 타이머 "5초 이전 선택 시 취소" | 없음 | 3D-003으로 커버 (초 수치가 null이라 매칭 실패) |
| REQ-03 C-05 저장 결과 | 없음 | ED-010(성공), ED-011(실패)로 커버 |
| REQ-04 layout 정렬 | 없음 | LIST-003으로 커버 (기대결과가 정렬 결과를 방 이름 순서로 적어 매칭 실패) |
| REQ-04 layout 말줄임 | 없음 | LIST-004로 커버 |

TC가 없는 영역은 **미채택 조건 6건이 다루던 부분**뿐이다. 요구사항 누락이 아니라 근거가 없어 질의로 넘긴 것이다.

| 미채택 조건 | 커버되지 않는 영역 |
|---|---|
| TCON-005 | 진입 이력 있는 계정의 비로그인 → 로그인 후 복귀 화면 |
| TCON-015 | 튜토리얼 진행 중 이탈 시 "계정당 1회" 소진 여부 |
| TCON-026 | 저장 없이 뒤로가기 시 이탈 확인 팝업·입력값 처리 |
| TCON-027 | 편집 중 세션 만료 상태의 저장 |
| TCON-030 | 백그라운드 복귀 시 입력값 유지 |
| TCON-040 | 목록 조회 자체 실패 |

### A03 예외 미판정
03a 예외 매트릭스 44건(4화면 × 11항목)은 전부 판정돼 있어 **미판정 항목은 없다.** 판정이 "질의 필요"인 6건은 TC가 없다.

| 화면 | 코드 | 상태 |
|---|---|---|
| 3D방 꾸미기 온보딩 | EX10 | 딥링크·푸시 진입 미기재 — TC 없음 |
| 신규 3D방 편집 | EX02 | 입력값 유지 미정의 (TCON-030 미채택) — TC 없음 |
| 신규 3D방 편집 | EX07 | 저장 시 세션 만료 미정의 (TCON-027 미채택) — TC 없음 |
| 신규 3D방 편집 | EX10 | 딥링크·푸시 진입 미기재 — TC 없음 |
| 내 3D방 목록 | EX01 | 목록에서 뒤로가기 이동 대상 미정의 — TC 없음 (LIST-010 기대결과 2에서 이동 화면을 적지 못한 원인) |
| 내 3D방 목록 | EX10 | 딥링크·푸시 진입 미기재 — TC 없음 |

### A04 재현 가능성
절차만 보고 제3자가 수행할 수 있다. 입력값은 모두 실제 값으로 적었다. 주의할 점 2가지.

- **데이터 세팅이 필요한 TC 13건** — LIST-002·003·004·005·006·008·009·010·011, ED-014·015·017, 그리고 LIST-001(0개 보유). 보유 방 개수, 방 이름 길이, 마지막 수정일을 미리 만들어야 한다. 사전조건에 값을 명시했다.
- **타이밍 정밀도가 필요한 TC 3건** — 3D-003(진입 4초 시점 선택), 3D-007(3초 시점 백그라운드 전환, 10초 후 복귀), ED-012(빠르게 3회 연속 선택). 수동 수행 시 오차가 결과를 바꿀 수 있다.

### A05 GWT 분리
아래 "GWT 자가 점검" 참고.

## 질의 필요 항목

### 03b에서 새로 나온 질의 (3건)
- **Q-17** 방의 "마지막 수정일"은 언제 갱신되나요? 저장 시점 기준인가요? (ED-014, FLOW-002) — 갱신되지 않으면 최근 수정순 정렬 결과도 달라진다
- **Q-18** 목록에서 기존 방을 선택해 편집 화면에 들어가면 저장된 방 이름이 입력창에 채워져 노출되나요? (LIST-007, ED-014)
- **Q-19** 온보딩 화면에서 닫기(X)로 나간 뒤 재진입하면 온보딩이 다시 노출되나요? Q-10 답변은 뒤로가기 기준이라 X에도 같게 적용되는지 확인이 필요합니다 (3D-008)

### 03a에서 이월된 질의 (8건)
조건 6건: Q-20(TCON-005 로그인 후 복귀 화면 충돌), Q-22(TCON-026 저장 없이 뒤로가기), Q-23(TCON-027 세션 만료), Q-24(TCON-015 튜토리얼 중 이탈), Q-25(TCON-030 백그라운드 입력값), Q-26(TCON-040 목록 조회 실패)
예외 매트릭스 2건: Q-21(목록 화면 뒤로가기 이동 대상), Q-27(딥링크·푸시 진입 — 3개 화면 EX10을 한 문항으로 묶음)

전체 목록은 `03b-questions.md` 참고. 전달용 문항은 11건이다(03b 신규 3 + 이월 8).

## 양식 프로파일 수정 요청
프로파일은 사람이 정하는 자산이라 임의로 고치지 않고 올렸다. 1번은 승인을 받아 반영했다.

1. ~~**`idAbbreviations`에 "전체 흐름" 약어가 없다.**~~ → **반영 완료 (2026-09-19)**. 사용자 승인으로 `"전체 흐름": "FLOW"` 추가. 오류 3건 해소.
2. ~~**`granularity.maxExpectedPerTc`가 4다.**~~ → **반영 완료 (2026-09-19)**. `granularity.exceptions`에 `{technique: "유스케이스", maxExpectedPerTc: 8}`을 추가했다. 원래 값 4는 샘플 24건에서 추정한 것이고 `confirmedByUser: false`라, 실제 회사 TC로 프로파일을 다시 뽑을 때 함께 확인하면 된다.

## 정답지 대비 (참고 — blind 테스트 아님)
수행자가 정답지 작성자와 동일하므로 공식 지표로 쓰지 않는다.

정답지 TC 23건은 모두 대응 TC가 있다. 3건은 이번에 2개로 분리했다.

| 정답지 | 이번 TC | 비고 |
|---|---|---|
| MENU-001 | MENU-001 | |
| 3D-001 | 3D-001 | |
| 3D-002 | 3D-002 | |
| 3D-003 | 3D-003 | |
| 3D-004 | 3D-005 | |
| 3D-005 | MENU-002 | 대분류를 전체 메뉴로 잡아 ID가 달라짐 |
| 3D-006 | MENU-003 | 대분류를 전체 메뉴로 잡아 ID가 달라짐 |
| 3D-007 | 3D-007 | |
| ED-001 | ED-001 | |
| ED-002 | ED-002, ED-003 | 건너뛰기 닫힘 / 새 방 재진입 미노출로 분리 |
| ED-003 | ED-004 | |
| ED-004 | ED-007 | |
| ED-005 | ED-009 | 정답지는 "기획 미기재, 입력 차단 가정"이었고 이번에는 Q-12 답변이 근거 |
| ED-006 | ED-010 | |
| ED-007 | ED-011 | |
| ED-008 | ED-012 | |
| ED-009 | ED-013 | |
| LIST-001 | LIST-001, LIST-011 | 빈 목록 구성 / 새 방 만들기 이동으로 분리 |
| LIST-002 | LIST-002, LIST-003 | 항목 구성 / 정렬로 분리 |
| LIST-003 | LIST-004 | 15자·16자 경계로 작성 (정답지는 18자 1건) |
| LIST-004 | LIST-005 | |
| LIST-005 | LIST-006 | |
| LIST-006 | LIST-008 | |

정답지에 없는 TC 17건: 3D-004, 3D-006, 3D-008, 3D-009, ED-005, ED-006, ED-008, ED-014, ED-015, ED-016, ED-017, LIST-007, LIST-009, LIST-010, FLOW-001, FLOW-002, FLOW-003

## GWT 자가 점검
분리 적용 후 When이 둘 이상인 TC: **LIST-009** 1건 (유지 결정) + FLOW-001·002·003 (유스케이스라 예외)

### 적용한 분리 (2026-09-19, 사용자 승인)
1차 점검에서 6건을 분리 검토 대상으로 올렸고, 5건을 분리했다. TC 39건 → 43건.

| 원래 TC | 분리 결과 | 이유 |
|---|---|---|
| 3D-005 | 3D-005(닫기 이동) + **3D-008**(재진입 온보딩 노출) | "나가기"와 "재진입 시 노출"이 서로 다른 판정. 근거도 다르다(p.2 / Q-10 유추) |
| 3D-006 | 3D-006(뒤로가기 이동) + **3D-009**(재진입 온보딩 노출) | 위와 동일 |
| ED-003 | ED-003(새 방 경로) + **ED-017**(기존 방 경로) | 경로 2개가 한 TC에 있었다. 분리하면서 미정의 구간(목록 뒤로가기 — Q-21)을 절차에서 뺐다 |
| LIST-001 | LIST-001(빈 목록 구성, 하) + **LIST-011**(새 방 만들기 이동, 중) | 우선순위가 달라 분리 기준에 해당 |
| ED-014 | ED-014(덮어쓰기 결과만) | 편집 화면 진입 확인은 LIST-007이 이미 본다. 새 TC를 만들지 않고 절차 1을 사전조건으로 옮겼다 |
| LIST-009 | 유지 | 9개→10개 상태 변화 뒤 제한이 걸리는지가 핵심이라 나누면 검증이 깨진다 |

ED-014는 나머지 절반이 기존 TC와 겹쳐 새 TC가 생기지 않았다. 그래서 44건이 아니라 43건이다.

FLOW-001·002·003은 유스케이스 TC라 When이 여럿인 것이 정상이며 분리 대상에서 제외했다.
3D-003, 3D-007, ED-001, ED-004, ED-007, ED-009, ED-010, ED-012, ED-015, ED-016, 3D-004, LIST-008, LIST-010은 연속 행동이 하나의 판정을 위한 것이라 예외로 뒀다.
